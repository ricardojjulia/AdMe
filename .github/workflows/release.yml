name: Release
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  deploy-staging:
    environment: staging
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Push migrations to staging
        run: |
          if [ -z "$SUPABASE_ACCESS_TOKEN" ] || [ -z "$STAGING_SUPABASE_PROJECT_REF" ]; then
            echo "::notice title=Supabase Staging Skipped::SUPABASE_ACCESS_TOKEN or STAGING_SUPABASE_PROJECT_REF secret is not configured. Skipping staging migration push."
            exit 0
          fi
          supabase db push --project-ref "$STAGING_SUPABASE_PROJECT_REF"
        env:
          STAGING_SUPABASE_PROJECT_REF: ${{ secrets.STAGING_SUPABASE_PROJECT_REF }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

  approve:
    needs: [deploy-staging]
    environment: production
    runs-on: ubuntu-latest
    steps:
      - run: echo "Approved"

  deploy:
    needs: approve
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Push migrations to production
        run: |
          if [ -z "$SUPABASE_ACCESS_TOKEN" ] || [ -z "$SUPABASE_PROJECT_REF" ]; then
            echo "::notice title=Supabase Production Skipped::SUPABASE_ACCESS_TOKEN or SUPABASE_PROJECT_REF secret is not configured. Skipping production migration push."
            exit 0
          fi
          supabase db push --project-ref "$SUPABASE_PROJECT_REF"
        env:
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      - name: Notify
        if: success()
        env:
          WEBHOOK_URL: ${{ secrets.DEPLOY_WEBHOOK_URL }}
        run: |
          if [ -n "$WEBHOOK_URL" ]; then
            curl -s -X POST "$WEBHOOK_URL" -H "Content-Type: application/json" \
              -d "{\"text\": \"✅ Deployed ${{ github.sha }} by ${{ github.actor }}\"}"
          fi
